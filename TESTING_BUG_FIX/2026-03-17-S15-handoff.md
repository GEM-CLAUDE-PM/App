# GEM & CLAUDE PM Pro — Session Handoff
**Ngày:** 2026-03-17 | **Session kết thúc:** S15 (95% complete)
**Repo:** github.com/GEM-CLAUDE-PM/App.git
**Deploy:** gemclaudepm.com (Vercel, auto-deploy)
**Stack:** React 19 + TypeScript + Tailwind + Vite 6 + Supabase (Singapore)

---

## SPRINT STATUS

| Sprint | Tên | Trạng thái |
|--------|-----|-----------|
| FS–S13 | Foundation + Supabase + Auth | ✅ CLOSED |
| S14 | Core features (BOQ, QAQC, Gantt, HSE) | ✅ CLOSED |
| **S15** | **Security + Multi-tenant + Portal + Billing** | **⚡ 95% — session này** |
| S16 | SaaS 🏆 Milestone 2 | ▶ NEXT |
| S17 | SaaS 🏆 Milestone 3 — Thu tiền | Planned |
| S18–S20 | Mobile + AI | Planned |

---

## ĐÃ HOÀN THÀNH TRONG SESSION NÀY

### Permission System (đột phá lớn)
- ✅ Role hierarchy v3.0 — 24 roles đầy đủ
- ✅ Union-roles logic: user được làm gì = union TẤT CẢ roles trong DA
- ✅ Permission matrix v2: domain-based, không phải level đơn thuần
- ✅ `getAccess()` v2 — 24 roles × 22 tabs đúng nghiệp vụ
- ✅ `maskSensitive` — che đơn giá/giá trị BOQ và HĐ với L3 trở xuống
- ✅ `project_member_overrides` — cấp quyền thêm theo từng DA
- ✅ `canSeeFullValues` từ authRoleId thực tế
- ✅ AdminPanel: multi-role per project (toggle buttons thay dropdown đơn)

### Auth Bug Sweep — 9 bugs / 7 files
- ✅ `App.tsx`: appCurrentRole từ authRoleId, không đọc localStorage
- ✅ `AuthProvider.tsx`: ghi 2 localStorage keys để tương thích
- ✅ `WorkspaceActionBar.tsx`: ROLE_CFG từ 8 → 24 roles
- ✅ `ProjectDashboard.tsx`: legacyToNew map đủ 24 roles, union roles, canSeeContractTab fix
- ✅ `QSDashboard.tsx`: LEVEL_MAP đủ 24 roles
- ✅ `MaterialsDashboard.tsx`: legacyRoleMap đủ 24 roles
- ✅ `PortfolioAnalytics.tsx`: allowedProjectIds từ useAuth()

### Dev Mode
- ✅ Dev Login Switcher: 18 mock users đủ role L1→L5
- ✅ `gem_user_role_dev_override` key riêng, xóa khi logout
- ✅ MOCK_USERS + MOCK_MEMBERS sync 24 roles
- ✅ `JOB_TO_ROLE` map đầy đủ, không fallback sai

### UI/UX
- ✅ Sidebar collapse/expand, nhớ trạng thái localStorage
- ✅ Logo mobile/desktop nhất quán (icon + GEM & CLAUDE + PM Pro)
- ✅ Tab order theo luồng nghiệp vụ: Tiến độ→Vật tư→Thiết bị→Giám sát→QA/QC
- ✅ Topbar sticky đúng trong scroll container
- ✅ GEM AI persona: bỏ "Nàng GEM Siêu Việt", giữ xưng "em"
- ✅ Notification UI: underline tabs, toggle size chuẩn, dashed add button
- ✅ Mobile: notification dropdown hoạt động, WorkspacePanel không bị crop

### Done Early (S16–S17 planned)
- ✅ SubconPortal + ClientPortal
- ✅ BillingPage + OnboardingFlow
- ✅ Signup flow + tenant creation
- ✅ Migration SQL: multi-tenant v2 + permission overrides

---

## CÒN LẠI — SẼ LÀM Ở S16

### Mandatory (prerequisite S16)
- 🔴 Multi-tenant RLS apply lên Supabase production (migration SQL đã có)
- 🔴 Trial badge + countdown + locked screen khi hết trial
- 🔴 pg_cron / Edge Function auto-lock expired tenants

### S16 Features
- Org chart Lớp 1: formal structure + chứng chỉ hành nghề (quyết định hôm nay)
- Transfer admin production test
- PayOS integration (VND payment)

### S17 Features
- Stripe + PayOS webhook thật
- Internal admin dashboard
- Org chart Lớp 2: activity presence (derived từ logs)

---

## MIGRATION SQL CẦN CHẠY TRÊN SUPABASE

```
/mnt/user-data/outputs/migration_multitenant_v2.sql     — tenants + billing_events
/mnt/user-data/outputs/migration_permission_overrides.sql — project_member_overrides
/mnt/user-data/outputs/migration_roles_v3_fixed.sql     — job_role enum 24 values
```

**Thứ tự chạy:** roles_v3 → multitenant_v2 → permission_overrides

---

## CRITICAL RULES (KHÔNG THAY ĐỔI)

```
dbLoaded = useRef(false)          — KHÔNG dùng useState
GEM_MODEL constant                — KHÔNG hardcode model string
db.set pattern với prev array     — KHÔNG set trực tiếp
notifErr() thay vì alert()        — KHÔNG dùng alert/console.log cho user
authRoleId từ useAuth()           — KHÔNG đọc localStorage cho role
allowedProjectIds từ useAuth()    — KHÔNG dùng getCurrentScopeCtx() cho filter
union roles (member.roles[])      — KHÔNG chỉ dùng activeRoleId cho permission
```

---

## TEST CHECKLIST CHO SESSION MỚI

- [ ] Login `gdda@villaphat.vn` → Giám đốc, thấy tất cả DA + tất cả tabs
- [ ] Login `thukho@villaphat.vn` → Thủ kho L1, chỉ thấy DA được gán + Vật tư tab
- [ ] Login `qs01@villaphat.vn` → QS site L2, BOQ hiện nhưng che đơn giá
- [ ] Login `cht@villaphat.vn` → CHT L3, thấy đầy đủ thi công, kế toán readonly
- [ ] Dev switcher 18 users hoạt động, role reset đúng khi logout
- [ ] WorkspaceActionBar hiển thị đúng role name (không còn "KS Giám sát" mặc định)
- [ ] Project list lọc đúng theo allowedProjectIds
- [ ] Sidebar collapse/expand nhớ trạng thái sau reload

---

## PROTOCOL SESSION MỚI

1. Upload `src.zip` (sau git pull)
2. Claude extract + verify esbuild clean
3. Đọc handoff này
4. Task đầu tiên S16: apply RLS migration lên Supabase production
5. Git trên Windows PowerShell — chạy lệnh sequential, KHÔNG dùng &&
```
